# Pinned so all teammates run identical versions. Setup:
#   python3 -m venv jac-env && source jac-env/bin/activate
#   pip install -r requirements.txt
# Keep in sync with jac.toml ([project] jac-version + [dependencies]).
jaclang==0.16.7
byllm==0.6.19
litellm==1.82.6
# Telephony (step 4): FastAPI webhook server + Twilio outbound dialing.
fastapi==0.121.3
uvicorn==0.38.0
twilio==9.10.9
# Required by FastAPI's request.form(); Twilio POSTs form-encoded webhooks, so
# without this BOTH /twiml/ endpoints 500 on the first request of every call.
python-multipart==0.0.32
